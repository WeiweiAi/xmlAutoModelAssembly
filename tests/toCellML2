import sys
    # caution: path[0] is reserved for script path (or '' in REPL)
sys.path.insert(0, '..')
from src.analyser import parse_model,analyse_model_full
from src.coder import writeCellML,writePythonCode

# This is a template for converting a CellML 1.X model to CellML 2.0

full_path='H:/ABI/12L/FAIRDOs/model/Boron-HCO3.cellml'
newPath='./csv/Boron-HCO3.cellml'

model_parse, issues=parse_model(full_path, False)
print(issues)
writeCellML(model_parse,newPath)
analyser,issues=analyse_model_full(model_parse,newPath,external_variables_info={},strict_mode=True)
print(issues)

# Only if the model is valid, then you can write the python code
"""
py_full_path='./csv/Boron-HCO3.py'
writePythonCode(analyser, py_full_path)

"""